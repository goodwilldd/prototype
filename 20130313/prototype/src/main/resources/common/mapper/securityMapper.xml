<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.pe.goodwilldd.security.mapper.normal.SecurityMapper">

	<select id="getUser" parameterType="string" resultType="AuthenticationModel">
		SELECT USER_NM FROM tb_cm_user WHERE USER_ID = #{userId}
	</select>

</mapper>